#!/usr/bin/env python3
import base64, gzip, pathlib
ROOT = pathlib.Path(__file__).resolve().parents[1]
# run: python3 scripts/install_stremio_meta.py
# full file written by companion parts below after git pull
print('Use VPS: curl the full file from artifacts or run after parts landed')
