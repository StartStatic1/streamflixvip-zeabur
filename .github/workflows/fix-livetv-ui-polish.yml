name: Fix Live TV UI Polish
on:
  workflow_dispatch:
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Patch
        run: python3 scripts/fix_livetv_ui_polish.py
      - name: Commit
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add android/app/src/main/java/com/streamflixvip/app/ui/livetv/
          git commit -m "fix(livetv): atalhos sem Todos, spinner mini só no load" || echo nothing
          git push
