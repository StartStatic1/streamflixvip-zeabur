name: fix-player-fenix-ua
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/fix_player_fenix_ua.py'
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply patch
        run: python3 scripts/fix_player_fenix_ua.py
      - name: Commit
        run: |
          git config user.name "streamflix-bot"
          git config user.email "bot@streamflix.local"
          git add android/app/src/main/java/com/streamflixvip/app/ui/player/PlayerScreen.kt || true
          if git diff --cached --quiet; then
            echo "no change"
          else
            git commit -m "fix: UA okhttp para streams Fenix/workers"
            git push
          fi
