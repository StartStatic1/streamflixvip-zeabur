name: Patch Player Gestures
on:
  workflow_dispatch:
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Apply gestures patch
        run: python3 scripts/patch_player_gestures.py
      - name: Commit and push
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add android/app/src/main/java/com/streamflixvip/app/ui/player/PlayerScreen.kt
          git diff --cached --stat
          git commit -m "feat(player): gestos volume (direita) e brilho (esquerda)" || echo "nothing"
          git push
