name: deploy-manual-final

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Apply API patches
        run: |
          patch -p1 < scripts/manual_final/live-tv.patch
          patch -p1 < scripts/manual_final/admin-vip.patch
          grep -c loadManualChannels api/live-tv.js
          grep -c list-live-tv-manual api/admin-vip.js
          head -3 api/live-tv.js
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add api/live-tv.js api/admin-vip.js
          git commit -m "feat: canais manuais na API (live-tv + admin-vip)" || echo nothing
          git push
