name: fix-assistir-only-with-sources
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/fix_assistir_only_with_sources.py'
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/fix_assistir_only_with_sources.py
      - name: Commit
        run: |
          git config user.name "streamflix-bot"
          git config user.email "bot@streamflix.local"
          git add android/app/src/main/java/com/streamflixvip/app/ui/detail/DetailScreen.kt || true
          if git diff --cached --quiet; then echo "no change"; else
            git commit -m "ux: Assistir so com fontes; loading sessao premium"
            git push
          fi
